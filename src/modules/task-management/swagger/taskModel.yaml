definitions: # Schema defination for request body
  task:
    type: object
    properties:
      id:
        type: string
      title:
        type: string
      description:
        type: string
        example: test
      isCompleted:
        type: boolean
        example: false
